{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "web-workspace",
      "runtimeExecutable": ".venv/bin/python",
      "runtimeArgs": ["-c", "from workspace_web.server import serve_workspace; from pathlib import Path; import os; serve_workspace(Path('output'), port=int(os.environ.get('PORT', '14173')))"],
      "port": 14173,
      "autoPort": true
    }
  ]
}
