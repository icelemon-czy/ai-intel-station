## MODIFIED Requirements

### Requirement: Runnable Documented Entrypoints
The `.ai` context SHALL document the same runtime surface and business structure that the workspace actually exposes.

#### Scenario: Use `.ai` as the runtime guide
- **WHEN** an operator or coding agent follows `.ai` runtime guidance
- **THEN** the guidance matches the unified operator surface and current business architecture without relying on stale mirrored workflow copies
