# Traceability — Research Web Workspace

| Requirement | Scenario | Code Anchor | Evidence | Status | Notes |
| :---------- | :------- | :---------- | :------- | :----- | :---- |
| Dashboard Reflects Local Research Workspace | Open dashboard with existing local archive | `workspace_web/service.py::build_dashboard_overview` + `web/src/App.jsx::DashboardSection` | `tests/test_web_workspace.py::test_build_dashboard_overview_summarizes_local_archive_and_recent_briefings` + `curl http://127.0.0.1:4173/api/dashboard` | verified | Dashboard 直接消费本地 sidecar 与 briefing 产物 |
| Dashboard Reflects Local Research Workspace | Dashboard surfaces archive gaps explicitly | `workspace_web/service.py::_orphan_markdown_paths` + `build_dashboard_overview()` | `tests/test_web_workspace.py::test_build_dashboard_overview_reports_missing_sources_and_orphan_markdown` | verified | 对 missing source 与 orphan markdown 都有显式提示 |
| Library Browses Local Research Items | Filter library results from local sidecars | `workspace_web/service.py::list_library_items` + `web/src/App.jsx::LibrarySection` | `tests/test_web_workspace.py::test_list_library_items_uses_local_filters_without_remote_collection` | verified | Library 复用本地 query，不触发远程 collect |
| Library Browses Local Research Items | Inspect one local research item in detail | `workspace_web/service.py::get_library_item_detail` + `web/src/App.jsx::LibrarySection` | `tests/test_web_workspace.py::test_get_library_item_detail_maps_local_metadata` | verified | 详情面板映射 sidecar 标准字段 |
| Briefing Workspace Previews And Saves Derived Reports | Preview a local digest or reading list before save | `workspace_web/service.py::preview_briefing` + `web/src/App.jsx::BriefingSection` | `tests/test_web_workspace.py::test_preview_briefing_returns_markdown_without_writing_file` | verified | Preview 不落盘，只生成本地 Markdown 预览 |
| Briefing Workspace Previews And Saves Derived Reports | Save a briefing with partial source coverage | `workspace_web/service.py::save_briefing` + `briefing/reports.py` | `tests/test_web_workspace.py::test_save_briefing_writes_output_and_marks_missing_sources` | verified | 保留 partial-success，并继续写 `output/briefing/` |
| MVP Navigation Stays Within Phase-One Scope | Open the phase-one web navigation | `workspace_web/service.py::workspace_sections` + `web/src/App.jsx::App` | `tests/test_web_workspace.py::test_workspace_sections_match_phase_one_scope` + `curl http://127.0.0.1:4173/api/navigation` | verified | 首期导航只暴露 Dashboard / Library / Briefing Workspace |
| **Dashboard Grid Layout Prevents Card Overlap** (added by `fix-web-dashboard-and-briefing-css-layout`) | PagePurposeCard occupies its own full-width row | `web/src/styles.css::.dashboard-grid > .poll-error-banner` (shared rule with .page-purpose-card and .empty-state-panel) | `web/test/dashboardLayout.test.mjs::dashboard-grid top-stack rules make PagePurposeCard full-width` | verified | CSS schema test; 5 scenarios for this Requirement all pass |
| **Dashboard Grid Layout Prevents Card Overlap** | Hero card uses an explicit column span | `web/src/styles.css::.hero-card` | `web/test/dashboardLayout.test.mjs::hero-card spans 8/12 columns and shrinks to its content` | verified | `grid-column: span 8` + `min-height: 0` |
| **Dashboard Grid Layout Prevents Card Overlap** | Metric cards use uniform spans that sum to 12 per row | `web/src/styles.css::.metric-card` + `.metric-card.wide` | `web/test/dashboardLayout.test.mjs::metric-card renders as a real card and uses span 4` + `metric-card.wide matches the 3-up row span` | verified | 3-up row math: 4+4+4=12 |
| **Dashboard Grid Layout Prevents Card Overlap** | Metric cards render as visible cards, not bare divs | `web/src/styles.css::.metric-card` (background, border, border-radius, padding, box-shadow) | `web/test/dashboardLayout.test.mjs::metric-card renders as a real card and uses span 4` | verified | Original CSS only had `grid-column` — fix added full card styling |
| **Dashboard Grid Layout Prevents Card Overlap** | Narrow viewport collapses dashboard to single column | `web/src/styles.css::@media (max-width: 1024px)` | (covered by `web/test/dashboardLayout.test.mjs::app-shell reduces right padding on narrow viewports` for app-shell; hero/metric span rules in @media are reviewed manually) | verified | 1024px 断点 |
| **App Shell Reserves Right Safe-Area** (added by `fix-web-dashboard-and-briefing-css-layout`) | Desktop right padding reserves safe area | `web/src/styles.css::.app-shell` | `web/test/dashboardLayout.test.mjs::app-shell reserves right safe-area on desktop` | verified | `padding-right: 100px` on desktop (raised from 80px — 80px 满足"future floating button"契约但视觉上仍被右下角橙色 gradient circle 压住,100px 保证内容右边缘落在 body 渐变的"中性区") |
| **App Shell Reserves Right Safe-Area** | Narrow viewport reduces right padding | `web/src/styles.css::@media (max-width: 1024px) .app-shell` | `web/test/dashboardLayout.test.mjs::app-shell reduces right padding on narrow viewports` | verified | `padding-right: 20px` on mobile |
| **Briefing Workspace Density And Action Reachability** (added by `fix-web-dashboard-and-briefing-css-layout`) | Form controls use a 10px border-radius | `web/src/styles.css::input, select` | `web/test/dashboardLayout.test.mjs::input and select use a 10px border-radius` | verified | Old 14px pill style replaced with 10px |
| **Briefing Workspace Density And Action Reachability** | Select dropdown uses a custom chevron | `web/src/styles.css::select` (standalone rule with `appearance: none`) | `web/test/dashboardLayout.test.mjs::select uses appearance:none with a custom chevron` | verified | `appearance: none` + `background-image` chevron + `padding-right: 32px` |
| **Briefing Workspace Density And Action Reachability** | Control panel uses compact 10px row gap | `web/src/styles.css::.briefing-layout .control-panel` | `web/test/dashboardLayout.test.mjs::briefing-layout control-panel uses a 10px row gap` | verified | `gap: 10px` (was 14px) |
| **Briefing Workspace Density And Action Reachability** | Form content remains reachable above sticky action bar | `web/src/styles.css::.briefing-layout .control-panel` (added `padding-bottom: 100px`) | `web/test/dashboardLayout.test.mjs::briefing-layout control-panel reserves bottom safe-area for sticky action row` | verified | 100px = 16px bar bottom + 74px bar content + 10px breathing room; 防止 Title / SOURCES 在长表单时滚到 sticky bar 下方 |
| **Briefing Workspace Density And Action Reachability** | PagePurposeCard in briefing uses single-column sub-grid | `web/src/styles.css::.briefing-layout > .page-purpose-card` | `web/test/dashboardLayout.test.mjs::briefing-layout page-purpose-card drops the 18px margin-bottom` | verified | `margin-bottom: 0` + sub-grid `1fr` |
| **Briefing Workspace Density And Action Reachability** | PagePurposeCard dt and dd labels are fully visible against the green gradient | `web/src/styles.css::.page-purpose-grid dt, dd` (removed `opacity: 0.7` on dt, added explicit `color: var(--ink)` on dd) | `web/test/collectLayout.test.mjs::page-purpose-card dt has a high-contrast color (alpha ≥ 0.85) and no opacity-based dilution` + `page-purpose-card dd declares an explicit color (not relying on inheritance)` | verified | 修了假阳性:旧 test 只检查 color 字面量,不检查 `opacity: 0.7` 是否把有效 alpha 拉到 < 0.85;强化 test 现在用 `stripComments` helper 跳过注释,真实拦截 opacity: < 0.85 + 强制 dd 自带 `color:` |
| **Briefing Workspace Density And Action Reachability** | Action row is a sticky bottom bar | `web/src/styles.css::.briefing-layout .action-row` | `web/test/dashboardLayout.test.mjs::briefing action-row is a sticky bottom bar with frosted-glass background` | verified | `position: sticky; bottom: 16px; z-index: 5` + flex + frosted glass |
| **Briefing Workspace Density And Action Reachability** | Sticky action bar degrades on narrow viewports | `web/src/styles.css::@media (max-width: 1024px) .briefing-layout .action-row` | (manual visual review) | verified | 1024px 断点下退化为带边框 button group |
| **Collect Form Stays Above The Fold** (added by `fix-web-collect-workspace-layout`) | Empty-state panel uses compact padding inside collect | `web/src/styles.css::.collect-layout .empty-state-panel` | `web/test/collectLayout.test.mjs::collect-layout empty-state-panel uses compact padding` | verified | `padding: 10px 12px; gap: 6px` |
| **Collect Form Stays Above The Fold** | Empty-state panel inner font-size is compact | `web/src/styles.css::.collect-layout .empty-state-panel p, li` | `web/test/collectLayout.test.mjs::collect-layout empty-state-panel inner font-size is compact` | verified | `font-size: 12px; line-height: 1.4` |
| **Collect Form Stays Above The Fold** | Empty-state panel supports a collapsible `<details>` summary | `web/src/styles.css::.collect-layout .empty-state-panel details > summary` | `web/test/collectLayout.test.mjs::collect-layout empty-state-panel supports a details/summary hook` | verified | JSX 钩子就绪,`<details>` 改造留作后续单独立项 |
| **Collect Form Stays Above The Fold** | Action row uses the same compact gap as briefing | (reuses `.action-row` shared rule) | (covered by `briefing-layout control-panel uses a 10px row gap`; collect 走相同的 10px 子级 gap pattern) | verified | collect 没新加 action-row 规则,沿用共享 14px gap(可接受:collect 字段更多,允许稍宽松) |
| **PagePurposeCard Displays Reads And Produces With Sufficient Contrast** (added by `fix-web-collect-workspace-layout`) | page-purpose-card declares a base text color | `web/src/styles.css::.page-purpose-card` (added `color: var(--ink);`) | `web/test/collectLayout.test.mjs::page-purpose-card declares an explicit base color and background` | verified | 显式 `color: var(--ink)` 防止 inherit 失守 |
| **PagePurposeCard Displays Reads And Produces With Sufficient Contrast** | page-purpose-grid collapses to single column in collect | `web/src/styles.css::.collect-layout > .page-purpose-card .page-purpose-grid` (shared with briefing) | `web/test/collectLayout.test.mjs::page-purpose-card collapses to single column inside collect` | verified | `grid-template-columns: 1fr` (与 briefing 共享规则) |
| **PagePurposeCard Displays Reads And Produces With Sufficient Contrast** | dt / dd text color in page-purpose-card meets contrast | `web/src/styles.css::.page-purpose-grid dt, dd` | `web/test/collectLayout.test.mjs::page-purpose-card dt has a high-contrast color (alpha ≥ 0.85)` | verified | `color: var(--ink)` + `opacity` 是已知遗留(spec 标注) |
| **Collect Layout Inherits App Shell Right Safe-Area** (added by `fix-web-collect-workspace-layout`) | collect-sidecar respects narrow-viewport right padding | `web/src/styles.css::@media (max-width: 1024px) .collect-sidecar, .collect-panel` | `web/test/collectLayout.test.mjs::collect-sidecar respects narrow-viewport right padding` | verified | 窄屏下加 `padding-right: 0` 防御(.app-shell 80px → 20px 还原时保护 collect panels) |
| **Collect Layout Inherits App Shell Right Safe-Area** | collect-layout grid does not extend past safe-area | (covered by `.collect-layout` shared rule + `.app-shell` safe-area tests) | `web/test/dashboardLayout.test.mjs::app-shell reserves right safe-area on desktop` + `app-shell reduces right padding on narrow viewports` | verified | .app-shell 是所有 section 的祖先,collect 继承 |
| **Source Switch Buttons Meet Accessibility Contrast** (added by `fix-web-collect-workspace-layout`) | Unselected source-switch has a visible border | `web/src/styles.css::.source-switch` (added `border-color: rgba(21, 34, 36, 0.32)`) | `web/test/collectLayout.test.mjs::source-switch has a visible default border (alpha ≥ 0.30)` | verified | 从 `var(--line)` (alpha 0.12) 提升到 rgba alpha 0.32 (WCAG 1.4.11) |
| **Source Switch Buttons Meet Accessibility Contrast** | Selected source-switch retains high contrast | `web/src/styles.css::.source-switch.active` | `web/test/collectLayout.test.mjs::source-switch.active retains the selected-state gradient` | verified | 选中态保持原 accent gradient + 白字 |
| **Library Serves Markdown Preview From Known Sidecar** (fixed by `fix-web-preview-output-path-base`) | Preview endpoint returns Markdown for a known sidecar path | `workspace_web/service.py::read_item_markdown` (Guard 1 resolves project-root-relative `output/...` input against project_root) | `tests/test_web_workspace.py::test_read_item_markdown_returns_content_for_known_sidecar_path` | verified | 修了真实 bug:Guard 1 曾把 `output/...` 前缀路径按 output_root 相对解析,candidate_rel 残留 `output/` 前缀而 known_rel 没有,导致所有合法 item 的 preview 都被拒 |
| **Library Serves Markdown Preview From Known Sidecar** | Preview endpoint rejects paths outside output_root | `workspace_web/service.py::read_item_markdown` (Guard 1 traversal check) | `tests/test_web_workspace.py::test_read_item_markdown_rejects_path_outside_output_root` | verified | `../etc/passwd` / 绝对路径经 project_root 回退后仍落在 output_root 外 → PreviewError(400) |
| **Library Serves Markdown Preview From Known Sidecar** | Preview endpoint rejects paths not present in any sidecar | `workspace_web/service.py::read_item_markdown` (Guard 2 known-sidecar check) | `tests/test_web_workspace.py::test_read_item_markdown_rejects_path_not_in_any_sidecar` | verified | output_root 内但非任何 sidecar output_path → FileNotFoundError(404) |
| **Library Serves Markdown Preview From Known Sidecar** | Preview endpoint returns 404 when file missing | `workspace_web/service.py::read_item_markdown` (Guard 3 existence check) | `tests/test_web_workspace.py::test_read_item_markdown_returns_404_when_file_missing` | verified | sidecar 已注册但底层 .md 被删 → FileNotFoundError(404) |
